import { allPossibleGroups } from '../src'

const numbers = range(32)
const pots = chunk(numbers, 8)

describe('numbers', () => {
  it('should work correctly for every combo', () => {
    const groups = []
    allPossibleGroups(pots, )
  })
})
